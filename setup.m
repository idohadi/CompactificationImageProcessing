%% Setup file
[p1, ~, ~] = fileparts(mfilename('fullpath'));
cd(p1);

%% Run external setup files
run(fullfile(p1, 'extern', 'SmallRotationToolbox', 'setup.m'));

%% Adds the necessary folders to path
addpath(p1);
dirs = {'alignment', ...
    'classification', ...
    'clebsch_gordan', ...
    'compactification', ...
    'estimation', ...
    'hMRA', ...
    'image_generation', ...
    'inversion', ...
    'rotation_invariant_bispectrum', ...
    'spherical_harmonics', ...
    'spherical_spectra', ...
    't_design'};
for J=1:length(dirs)
    addpath(genpath(fullfile(p1, dirs{J})));
end

%% Adding ASPIRE's path
cd('../aspire/');
initpath;
cd('../SphericalInvariantAnalysis/');
cd('../manopt');
importmanopt;